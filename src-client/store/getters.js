export const isLogged = state => state.login.isLogged;

export const loggedUser = state => state.login.loggedUser;

export const userToLog = state => state.login.userToLog;

export const errorMessage = state => state.login.errorMessage;